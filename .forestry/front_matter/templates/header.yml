---
label: Header
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
  default: Header Section
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
pages:
- _data/header.yml
